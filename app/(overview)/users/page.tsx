import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Study Jam | Users",
};

const Users = () => {
  return (
    <div>
      <h1 className="text-lg font-bold">Users</h1>
    </div>
  );
};

export default Users;
